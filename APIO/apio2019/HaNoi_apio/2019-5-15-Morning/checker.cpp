#include<bits/stdc++.h>
using namespace std;

int main() {
	int TTest = 300;
	while (TTest--) {
		system("draft.exe");
		system("C_C.exe");
		system("C.exe");

		if ( system("fc answer.txt output.txt") ) { cout << "WA \n"; exit(0); }
		cout << "PASS \n";
	}
	cout << "AC \n";

	return 0;
}