#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("BinaryRule.INP", "r", stdin);
    freopen("BinaryRule.OUT", "w", stdout);
    int n; cin >> n;
    if (n == 0) cout << 0;
    else {
        while (n--) cout << 1;
    }
}